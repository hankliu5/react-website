{
  "name": "react-website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "marked": "^0.4.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-marked": "^0.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  },
  "homepage": "https://hankliu5.github.io/react-website",
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
